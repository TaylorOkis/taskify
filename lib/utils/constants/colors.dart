import 'package:flutter/material.dart';

class TaskifyColors {
  TaskifyColors._();

  // Background Colors
  static const Color lightBackgroundColor = Color(0xFFFEFEFE);
  static const Color darkBackgroundColor = Color(0xFF07090F);

  // App Colors
  static const primaryColor = Color(0xFF5969FD);
  static const secondaryColor = Color(0xFF32373B);
  static const tertiaryColor = Color(0xFF004932);

  // Text Colors
  static const lightText = Colors.white;
  static const darkText = Colors.black;

  static const lightGrey = Color(0xFF737373);
}
